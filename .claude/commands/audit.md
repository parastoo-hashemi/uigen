your goal is to update any vulnarable dependencies.

Do the following:
1. Run `npm audit` to find vulnarable installed packages in this project.
2. run `npm audi fix` to apply updates 
3. run tests and verify the updates didn't break anything 
